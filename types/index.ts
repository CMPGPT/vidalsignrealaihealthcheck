// This file contains necessary type definitions
// It replaces the need for separate .d.ts declaration files

// Admin component types
export interface AdminSettingsProps {
  defaultTab?: string;
}

// Add any other necessary types here that might have been defined in declaration files 