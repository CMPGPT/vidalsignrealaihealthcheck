'use client';

import React from "react";
// Import with full path to ensure TypeScript finds it
import AdminSettings from "@/components/admin/AdminSettings";

export default function SettingsPage() {
  return (
    <div>
      
      <AdminSettings />
    </div>
  );
} 